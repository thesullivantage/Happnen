import React from "react";
import "./RequestInviteBtn.css";
import {Button} from "react-materialize";

const RequestInviteBtn = props => (
<Button className="request-btn" {...props}>
Add Event
</Button>
);

export default RequestInviteBtn;