import React from "react";
import "./Grey.css";
import { Button } from "react-materialize";

const Grey = props => (
	<Button className="accept-btn" {...props}>
		Accept
	</Button>
);

export default Grey;